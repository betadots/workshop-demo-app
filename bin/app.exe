#!/bin/bash
if [ "$1" == "list" ]; then
  cat /opt/app/etc/app.cfg.yml
  exit 0
fi
if [ "$1" == "set" ]; then
  echo "Dummy: Setzen"
  exit 0
fi
if [ "$1" == "version" ]; then
  echo "app version: 3.1.2-git-e4f10ac"
  exit 0
fi

echo "Usage: $0 ['list', 'set', 'version']"

